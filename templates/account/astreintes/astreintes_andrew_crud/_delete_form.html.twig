<form method="post" action="{{ path('app_astreintes_andrew_crud_delete', {'id': astreintes_andrew.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ astreintes_andrew.id) }}">
    <button class="btn">Delete</button>
</form>
