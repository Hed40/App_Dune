<form method="post" action="{{ path('app_account/astreintes/delete', {'id': astreintes_users.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ astreintes_users.id) }}">
    <button class="btn">Delete</button>
</form>
